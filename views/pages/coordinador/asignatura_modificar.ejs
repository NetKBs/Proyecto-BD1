<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coordinador - Asignaturas</title>
    <link rel="stylesheet" href="/css/init.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/header_footer.css">
    <link rel="stylesheet" href="/css/coordinador/asignatura_home.css">
    <link rel="stylesheet" href="/css/coordinador/representante.css">
    <style>z
        /* Estilos para el contenido principal cuando hay un sidebar*/
        .content-main {
            margin-left: 220px;
            padding: 10px;
            margin-top: 20px;
            margin-bottom: 50px;
        }
    </style>
</head>
<body>
    <header>
        <div id="left-header">
            <a href="#"><img src="/images/logo.png" alt="logo"></a>
            <h1>Coordinador Abiham Ramos</h1>
        </div>
    
        <!-- OPCIONES DEL HEADER ACA -->
        <div id="right-header">
            <a href="#">Salir</a>
        <div>
    </header>
    
    <nav id="sidebar">
        <!-- OPCIONES DE LA SIDEBAR ACA -->
         <a href="#">Inicio</a>
         <a href="#">Periodos</a>
         <a href="#" class="actual-link-sidebar">Asignaturas</a>
         <a href="#">Cargas Académicas</a>
         <a href="#">Inscripción</a>
         <a href="#">Calificaciones</a>
         <a href="#">Docentes</a>
         <a href="#">Estudiantes</a>
         <a href="#">Representantes</a>
    </nav>
    
    <!-- ACÁ IRÁ EL CONTENIDO QUE SE MOSTRARÁ EN EL MEDIO DE LA PAGINA -->
    <main class="content-main">
        <div class="header-main">
            <a href="/coordinador/asignatura" class="nuevo">Regresar</a>   
        </div>
        <div class="card-agregar">
            <h3>Actualizar Asignatura</h3>
            <form action="/coordinador/asignatura/editar/<%= data.asignatura.id %>" method="POST">
                <label for="asignatura">Nombre de la asignatura</label>
                <input type="text" name="asignatura" id="asignatura" value="<%= data.asignatura.nombre %>">
                <label for="codigo">Codigo</label>
                <input type="text" name="codigo" value="<%= data.asignatura.codigo %>">
                
                <fieldset>
                    <legend>Años en los que se imparte</legend>
                    <label>
                        <input type="checkbox" name="grados" value="1" <% if(data.asignatura.grados.includes(1)) { %> checked <% } %>> Grado 1
                    </label>
                    <label>
                        <input type="checkbox" name="grados" value="2" <% if(data.asignatura.grados.includes(2)) { %> checked <% } %>> Grado 2
                    </label>
                    <label>
                        <input type="checkbox" name="grados" value="3" <% if(data.asignatura.grados.includes(3)) { %> checked <% } %>> Grado 3
                    </label>
                    <label>
                        <input type="checkbox" name="grados" value="4" <% if(data.asignatura.grados.includes(4)) { %> checked <% } %>> Grado 4
                    </label>
                    <label>
                        <input type="checkbox" name="grados" value="5" <% if(data.asignatura.grados.includes(5)) { %> checked <% } %>> Grado 5
                    </label>
                </fieldset>

                <input type="submit" value="Guardar">
            </form>
        </div>

    </main>
    
    
    <footer>
        <p>&copy; 2024. Colegio Santa Marta. Todos los derechos reservados.</p>
    </footer>
    
</body>
</html>