const coordinadorModels = require('../models/coordinadorModels')
const generalModels = require('../models/generalModels')

exports.coordinador = async (req, res) => {
    res.send('<h1>Coordinador</h1>')
}

